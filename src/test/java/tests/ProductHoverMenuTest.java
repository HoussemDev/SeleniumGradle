package tests;

import pages.HomePage;

public class ProductHoverMenuTest extends TestBase {
	HomePage homeObject;
	/*
	 * @Test public void UserCanSelectSubCategoryFromMainMenu() { homeObject = new
	 * HomePage(driver); homeObject.selectNoteBooksMenu();
	 * Assert.assertTrue(driver.getCurrentUrl().contains("notebooks"));
	 * 
	 * }
	 */
}
